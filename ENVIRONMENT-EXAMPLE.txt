# Авторизация редактора хранится в D1, а не в Runtime variables / secrets.
# Не публикуйте реальные пароли или SQL с рабочими хэшами в репозитории.

# Серверные привязки
DB=D1_or_compatible_database
BUCKET=R2_or_compatible_object_storage_optional
