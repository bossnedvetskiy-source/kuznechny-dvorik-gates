(() => {
  const COLORS=[{id:'chocolate',label:'Шоколад'},{id:'graphite',label:'Графит'},{id:'moss',label:'Зелёный мох'},{id:'mint',label:'Зелёная мята'},{id:'wine',label:'Винно-красный'}];
  const stateText=text=>{const state=document.getElementById('colorPhotoState');if(state)state.textContent=text;};
  const colorForTarget=target=>{const card=target.closest('.color-photo-card'),grid=card?.parentElement,index=grid?[...grid.children].indexOf(card):-1;return index>=0?COLORS[index]:null;};
  const refresh=(article,colorPhotos)=>{if(galleries?.[article])galleries[article].colorPhotos={...(colorPhotos||{})};if(activeArticle===article)document.dispatchEvent(new CustomEvent('admin:ready'));};
  document.addEventListener('change',async event=>{const input=event.target;if(!(input instanceof HTMLInputElement)||!input.matches('.color-photo-upload input[type="file"]'))return;event.stopImmediatePropagation();const file=input.files?.[0];input.value='';if(!file)return;const color=colorForTarget(input),article=activeArticle;if(!color||!article)return;if(!photoUploadEnabled)return showToast('Загрузка фотографий временно недоступна',true);stateText(`Загружаем: ${color.label}…`);try{const blob=await optimizeImage(file);const data=await api(`/api/admin/catalog-color-upload/${encodeURIComponent(article)}/${encodeURIComponent(color.id)}`,{method:'POST',headers:{'content-type':'image/webp'},body:blob});refresh(article,data.colorPhotos);showToast(`${color.label}: фото опубликовано`);stateText('Фото цвета сохранено');}catch(error){showToast(error.message,true);stateText('Не удалось сохранить фото');}},true);
  document.addEventListener('click',async event=>{const button=event.target.closest?.('.color-photo-remove');if(!button)return;event.preventDefault();event.stopImmediatePropagation();if(button.disabled)return;const color=colorForTarget(button),article=activeArticle;if(!color||!article)return;if(!confirm(`Удалить фото цвета «${color.label}» для ${article}?`))return;button.disabled=true;stateText(`Удаляем: ${color.label}…`);try{const data=await api(`/api/admin/catalog-color-slot/${encodeURIComponent(article)}/${encodeURIComponent(color.id)}`,{method:'DELETE'});refresh(article,data.colorPhotos);showToast(`${color.label}: фото удалено`);stateText('Фото цвета удалено');}catch(error){showToast(error.message,true);stateText('Не удалось удалить фото');button.disabled=false;}},true);
})();
